export type UsagePayload = {
  inputTokens?: number;
  outputTokens?: number;
  totalTokens?: number;
};

export type Usage = {
  inputTokens: number;
  outputTokens: number;
  totalTokens: number;
};
