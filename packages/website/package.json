{
  "name": "@parchi/website",
  "version": "0.4.10",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx wrangler dev --config wrangler.toml",
    "deploy": "npx wrangler deploy --config wrangler.toml"
  }
}
